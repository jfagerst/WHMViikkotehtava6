# Viikkotehtävä 6

### Barcodes

Create an app that can read barcodes (EAN). App should use phone’s camera, read barcode and display it in number format. Use documentation available on
https://docs.expo.dev/versions/latest/sdk/camera/
